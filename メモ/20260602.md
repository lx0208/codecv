お疲れ様です。

先日、システム全体の構成図を作成いたしました。

結論として、Load Balancerサーバー上で以下の3つのコンテナを稼働させる想定です。

OpenWebUI
LiteLLM
PostgreSQL

また、既存の2台のDGX Sparkでは、同一構成の vLLM + モデルを稼働させます。

LiteLLM Gatewayをロードバランサーノードとして利用し、2台のDGX上のvLLMへ接続する構成となります。

そのため、Load Balancerサーバーについて、以下の点をご対応いただけますでしょうか。

Linuxサーバーのご用意（メモリ8GB以上、ストレージ500GB以上）
Dockerのインストール
既存の2台のDGX Sparkとのネットワーク接続
サーバーのIPアドレスおよびログイン情報のご共有（今後の作業でログインが必要となります）

また、新しいTask一覧を作成し、下記ページへ移動いたしました。

xxx

ご確認のほど、よろしくお願いいたします。